import styles from '../styles/components/Button.module.scss';

const Button = () => {
  return (
    <>
      <button className={styles.error}>Click me</button>
    </>
  );
};

export default Button;
